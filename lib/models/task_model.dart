class Task {
  final String id;
  final String name;
  final bool isCompleted;
  final int points;

  Task(
      {required this.id,
      required this.name,
      required this.isCompleted,
      required this.points});
}
